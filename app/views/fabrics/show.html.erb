<div class="page-header">
  <h3>Fabric #<%= @fabric.id %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Diseno</dt>
      <dd><%= @fabric.diseno %></dd>

      <dt>Peso</dt>
      <dd><%= @fabric.peso %></dd>

      <dt>Ancho</dt>
      <dd><%= @fabric.ancho %></dd>

      <dt>Precio</dt>
      <dd><%= @fabric.title %></dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/fabrics" class="btn btn-primary">
        Back
      </a>
      <a href="#" class="btn btn-default">Add to Order</a>
      <!-- <a href="/fabrics/<%= @fabric.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_fabric/<%= @fabric.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a> -->
    </div>
  </div>
</div>
